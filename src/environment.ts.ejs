<%
if (model && modelPath) {
%>
import { <%= model %> } from '<%= modelPath %>';
export const environment: <%= model %> = <%= environment %>;
<%
} else {
%>
export const environment = <%= environment %>;
<%
}
%>
